package advanced.module6.contest2023_11_03;

/**
 * Subsequences that aren't palindromes can be removed in a single stroke.
 * Any character set that has the original order, that needn't be contiguous are subsequences
 * Objective is to find the minimum no. of strokes to remove the entire string
 * The given string can only contain 'a' or 'b', and the count of the string is even
 * Also, at least 1 a and 1 b exists in the string.
 */
public class NotPalindromesRemoved {
    //Effectively, if the given string is palindrome return 2, else return 1;

}
